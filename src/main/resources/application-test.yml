spring:
  flyway:
    locations: classpath:db/migration
    ignore-migration-patterns: "repeatable:missing"

app:
  security:
    rate-limit:
      login:
        enabled: false

logging:
  level:
    com.cotaseguro: INFO
